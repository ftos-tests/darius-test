on: push
jobs:
  git-sync:
    runs-on: ubuntu-latest
    steps:
      - name: git-sync
        uses: wei/git-sync@v3
        with:
          source_repo: "https://dariusxdragoi:PERSONAL_ACCES_TOKEN@github.com/ftos-tests/darius-testA.git"
          source_branch: "main"
          destination_repo: "ftos-tests/darius-testB"
          destination_branch: "main"
          ssh_private_key: ${{ secrets.SSH_PRIVATE_KEY }}
